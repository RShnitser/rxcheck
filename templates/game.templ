package templates

import(
"rxcheck/internal/database"
)

templ Game(classificatons []database.Classification){
    <div>
     for _, classification := range classificatons {
        <div>{ classification.Name }</div>
    }
    </div>
}