package templates

import(
    "fmt"
)

templ Explanation(explanation string, nextQuestion int32){
    <div
        hx-target="this"
        hx-swap="outerHTML"
    >
        <div>{explanation}</div>
        <a hx-get={fmt.Sprintf("/question/%d", nextQuestion + 1)}>Next Question</a>
    </div>
}